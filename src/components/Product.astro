---
import {Image} from "astro:assets";

interface Props {
  name: string;
  price: number;
  imgUrl: string;
  slug: string;
}

const {name, price, imgUrl, slug} = Astro.props;

console.log(imgUrl);
---

<article>
  <a href=`/producto-static/${slug}`>
    <Image src={imgUrl} alt={name} height={200} width={200} />
    <hgroup>
      <h4>{name}</h4>
      <p>{price}€</p>
    </hgroup>
  </a>
  <a href="#" role="button">Add to cart</a>
</article>

<style>
  article {
    text-align: center;
    line-height: 1.1;
  }
  hgroup {
    h4 {
      font-size: 1rem;
    }
  }
</style>
